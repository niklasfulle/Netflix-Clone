services:
  app:
    image: salkin263/netflix-clone:${APP_VERSION:-latest}
    container_name: netflix-clone
    ports:
      - "3000:3000"
    volumes:
      - /movies:/movies
      - /series:/series
    restart: unless-stopped

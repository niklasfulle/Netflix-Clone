# Ansible target configuration
# Save separate copies as .env (production) and .env.staging (staging).
LXC_HOST=
LXC_USER=root
LXC_PORT=22
# Optional. Defaults to netflix (production) or netflix-staging (staging).
HTTPS_HOST=
